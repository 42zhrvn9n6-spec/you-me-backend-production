import { calculateNatalChart, findHouse, angularDistance } from '../src/services/ephemeris.service';
describe('Swiss Ephemeris regression',()=>{test('calculates a natal chart',async()=>{const c=await calculateNatalChart('1990-01-01','12:00','Izmir',38.423,27.142,120,'Placidus','Europe/Istanbul');expect(c.planets.length).toBeGreaterThanOrEqual(10);expect(c.houses).toHaveLength(12);expect(c.calculationMeta).toBeDefined();});test('findHouse handles wrap',()=>expect(findHouse(359,[350,20,50,80,110,140,170,200,230,260,290,320])).toBe(1));test('distance wraps',()=>expect(angularDistance(359,1)).toBe(2));},30000);});
